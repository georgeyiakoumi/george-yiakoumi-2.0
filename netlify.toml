# Storybook-only deployment (no Next.js plugin)

[build]
  command = "npm run build-storybook"
  publish = "storybook-static"

[build.environment]
  # Disable Next.js framework detection
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
